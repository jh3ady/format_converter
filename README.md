# Format Converter

## How to install

```bash
python3 setup.py install
```

## How to use it

```bash
format_converter -i "<input_file>" -if "<input_type>" -o "<output_file>" -of "<output_type>"
```

Formats available:
* csv
